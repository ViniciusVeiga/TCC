@model ETHistoric

@using TCC.Domain.Entities
@using TCC.BusinessLayer.BusinessLayers

@{
    var urlSave = "Learnboard/UserStory/SaveHistoric";
    var page = "Page_2";
}

@Scripts.Render("~/Scripts/Postit")

<script type="text/javascript">
    var button;

    function start() {
        button = '';
        //setRandomColor();
    }

    function writeLine() { }

    function sendToSave(event) {  }

    function validationsByJs() { }

    function count() {  }

    function validationsByJs() { }

    function finishCard() {  }

    function appendPostit(event) {  }

    function removePostit(event) {  }

    function removeLine(event) { }

    $('.postit.select').on('click', function () {
        $(this).children('.close').html(button);
    });

    function getRandomColor() {
        var letters = '0123456789ABCDEF';
        var color = '#';
        for (var i = 0; i < 6; i++) {
            color += letters[Math.floor(Math.random() * 16)];
        }
        return color;
    }

    function setRandomColor() {
        var color = getRandomColor();

        $('#ColorButton i').css("color", color);
        button = $('#ColorButton').html();
    }

    $('#ColorButton').click(function () {
        setRandomColor();
    });
</script>